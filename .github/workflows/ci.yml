name: quality

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install project and quality tools
        run: python -m pip install -e ".[dev]"
      - name: Lint
        run: python -m flake8 medboard tests app.py
      - name: Type check
        run: python -m mypy medboard
      - name: Test with coverage
        run: python -m pytest --cov=medboard --cov-report=term-missing
      - name: Validate installed dependencies
        run: python -m pip check
